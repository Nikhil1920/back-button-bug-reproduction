import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
    appId: "in.anreddy.test",
    appName: "test",
    webDir: "out",
    bundledWebRuntime: false,
};

export default config;
